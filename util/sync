#!/bin/bash -e
# (this should be run from the root of the repo: util/sync)
# Before pulling, you need to run add once (same command line but with add).
set -e
cd $HOME/agora/garden
for i in *; do
    cd $i
    git pull
    cd ..
done
